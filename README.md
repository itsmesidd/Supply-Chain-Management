# Supply-Chain-Management
“I built a Python-based ETL pipeline that processes raw supply chain transaction data from CSV files, cleans and transforms it, and loads it into SQL tables. The focus was on making the data analytics-ready so trends and patterns could be analyzed easily.
